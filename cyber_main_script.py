# Import individual scripts on startup
# To run at startup, open the crontab editor:   bash "crontab -e:  and add the following line to the crontab file:  "@reboot sleep 10 && python /path/to/your/script.py"
